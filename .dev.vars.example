# Copy to .dev.vars and use development-only values. Never reuse production secrets.
REGISTRATION_INVITE_CODE=replace-with-local-invite-code
PASSWORD_HMAC_KEY=replace-with-at-least-32-random-bytes
SESSION_HMAC_KEY=replace-with-a-different-at-least-32-random-bytes
