# 台球计分应用：后续总路线图

更新时间：2026-08-16

当前状态：R4 / v5.1.0 已更新至既有生产 Worker；角色切换权限收敛、对局级成员投影与主“对局”层入口（A5/A6）及 WebSocket 101 连接回归已在本地实现并通过集成测试，剩余预览环境 15 客户端容量与用量观察

R3 技术决策基线：[docs/R3_TECHNICAL_DECISIONS.md](docs/R3_TECHNICAL_DECISIONS.md)

---

## 1. 当前结论

后续版本按以下顺序推进：

1. **R2.5 / v4.1.0：中八双人计分板与中八/追分统一战绩导出（已发布）**
2. **R3 / v5.0.0：账号、Cloudflare D1 与跨设备同步（已验收）**
3. **R4 / v5.1.0：多人同时参加一局并实时共同操作**
4. **R5 / v5.2.0：牌组、历史、统计与分享**
5. **R6 / v6.x：新玩法与娱乐模式**

关键边界：

- R2.5 已于 `v4.1.0` 完成并发布，当前开发主线已切换到 R3。
- R2.5 复用现有追加式事件和更正机制，避免 R3 云同步时再次迁移数据模型。
- R3 与 R4 连续设计，但不合并开发和发布。
- R3 先解决账号、权限、云端持久化、迁移和单设备写入，并预留 R4 所需的数据与命令协议。
- R4 在 R3 稳定后引入 Durable Objects 和 WebSocket，承担实时房间、顺序写入、广播和重连。
- 15 人左右的私用规模，按 Cloudflare 当前免费额度预计可保持零成本；上线后仍需观察实际请求量和持久化用量。

---

## 2. 已完成基线

### R1 / P0：发布护栏与首期收口（已验收）

- [x] GitHub CI 覆盖 lint、测试和构建
- [x] 桌面与移动端关键流程验收
- [x] 未结束对局的继续、保存后新建和放弃流程
- [x] 本地数据异常恢复和安全重置入口
- [x] 正式部署、冒烟测试和回滚流程

### R2 / v4.0.0：本地核心功能完整版（已完成）

- [x] 完整奇招、轻量、竞技和安全等官方牌组
- [x] 每场保存牌组版本与完整快照，旧对局不受后续修改影响
- [x] 2—8 人本地多人计分
- [x] 玩家分别设置初始积分、中途加入、离场和重新排序
- [x] 固定轮转、胜者开球等顺序策略
- [x] 转账型计分、自定义计分项和特殊规则
- [x] 事件备注、补录和保留原记录的更正机制
- [x] 手动抽牌、自动补牌、手牌上限和牌库耗尽处理
- [x] 卡牌事件与计分事件联动、撤销和时间线恢复
- [x] 本地历史、未结束对局恢复和旧版本数据迁移

现有本地玩法和离线能力必须在后续版本继续可用，不能因接入云端而退化。

---

## 3. R2.5：中八双人计分板与统一战绩导出（已发布）

目标版本：**v4.1.0**

状态：**已完成并发布于 v4.1.0**

前置条件：R2 本地事件模型和恢复能力已完成

### R2.5.1 产品定位

- 面向中式八球双人比赛，独立于现有追分和奇招牌模式
- 中八计分板提供上下二等分和左右二等分两种显示布局
- 中八与追分共用统一战绩文档框架和三种导出方式
- 手机竖屏优先，同时兼容横屏、平板和桌面浏览器
- 未登录即可完整使用，所有核心功能离线可用
- 后续登录时可由 R3 迁移和同步，不把账号系统作为本版本前置条件

### R2.5.2 开局设置

- 可修改双方姓名，默认显示“玩家 A”和“玩家 B”
- 可选择抢 N 局或不限局数的自由计分
- 可设置先开球方，并选择轮流开球或胜者开球
- 可选择计分板布局：上下二等分或左右二等分
- 可填写比赛名称、地点和备注；全部为可选项
- 开始后记录创建时间、开赛时间和计时状态

默认实现决定：第一版只支持两名选手，不在此版本加入三人以上的中八赛制。

### R2.5.3 计分板界面

- 顶栏显示“第 N 局”和比赛累计用时
- 红蓝两块区域分别显示姓名、当前总局分和大号数字
- **上下二等分**：玩家 A 在上、玩家 B 在下，中间使用横向分隔线
- **左右二等分**：玩家 A 在左、玩家 B 在右，中间使用纵向分隔线
- 比赛进行中允许切换布局，切换只改变显示，不产生计分事件
- 记录当前比赛的布局偏好，刷新后保持；新比赛默认沿用设备最近一次选择
- 两种布局都必须严格均分主要计分区域，不能因姓名长度或统计项数量挤压另一方
- 每名玩家持续显示：普胜、炸清、接清、犯规累计次数
- 提供暂停/继续计时、查看流水和结束比赛入口
- 姓名允许在比赛中修改，但改名必须写入流水，旧事件仍能识别同一名选手
- 重要操作使用明确按钮或操作面板，不把整个大色块作为无确认的加分按钮
- 计分后提供短时撤销入口，防止触屏误操作

### R2.5.4 每小局记录流程

每完成一小局，通过一个操作面板记录：

1. 选择本局胜者
2. 选择胜局类型：`普胜`、`炸清`、`接清`
3. 分别设置双方本局犯规次数，默认均为 0
4. 可选填写本局备注
5. 确认后胜者总局分加 1，统计同步累计，并进入下一局

规则约束：

- 普胜、炸清、接清是单局胜法，同一小局只能选择一种
- 犯规属于双方各自的单局事件，可记录 0 次、1 次或多次
- 炸清或接清只计入胜者的对应统计
- 单局确认后自动保存，刷新或意外关闭页面不得丢失已确认记录
- 达到抢 N 目标时提示结束比赛，但由用户最终确认，避免误结算

### R2.5.5 流水、更正与恢复

每一小局至少保存：

- 稳定的比赛 ID、局序号和事件 ID
- 当时的双方姓名和稳定选手 ID
- 开球方、胜者和胜局类型
- 双方犯规次数
- 操作前比分、操作后比分
- 本局开始时间、确认时间、耗时和备注
- 创建、撤销或更正来源

更正规则：

- 可以撤销最后一局，也可以从流水中更正指定小局
- 更正生成新的补偿/更正事件，不静默覆盖或删除原事件
- 更正后重新计算总比分、普胜、炸清、接清和犯规累计
- 本地保存一份当前快照，同时保留可复算的完整事件流水
- 应用刷新、离线和浏览器异常退出后可继续未结束比赛

### R2.5.6 中八与追分的统一战绩导出

中八和追分结束后都生成相同结构、相同视觉规格的战绩文档。统一部分包含：

- 比赛名称、玩法、时间、地点、赛制和总耗时
- 所有玩家姓名、最终结果、排名或获胜者
- 按实际发生顺序排列的完整事件流水
- 备注、撤销和更正记录，确保最终结果可追溯

中八战绩另外包含：

- 双方姓名、最终比分和获胜者
- 双方普胜、炸清、接清、犯规总计
- 按局排列的完整流水：局号、开球方、胜者、胜法、双方犯规、比分变化、用时和备注

追分战绩另外包含：

- 所有玩家的初始分、最终分、净变化和最终排名
- 每条计分流水的时间、计分项、操作者、涉及玩家、分值变化和变化后余额
- 转账型计分的来源玩家、目标玩家及双方分数变化
- 自定义计分项、事件备注、补录、牌分联动和关联卡牌事件
- 中途加入、离场、改名、调整顺序及受控更正记录

两种玩法都提供以下三种导出：

1. **战绩长图**：适合保存到相册或聊天分享
2. **打印友好页面**：浏览器可直接打印或另存为 PDF
3. **JSON 数据备份**：保留全部原始事件，供恢复和未来云同步

CSV 可在后续按实际使用需求增加，不作为 v4.1.0 发布阻断项。

### R2.5.7 数据模型与 R3/R4 衔接

- 中八比赛仍进入统一 `matches` 体系，并以明确的玩法类型区分
- 双方使用稳定 `match_player_id`，显示姓名不是关联主键
- 每次计分、犯规、撤销和更正都使用追加式事件
- 预留 `operation_id`、`sequence_no` 和 `match_version`
- R3 上线后可把本地中八比赛迁移到 D1
- R4 上线后可复用相同业务命令，实现两台设备共同操作中八计分板
- R2.5 本身不引入账号、D1、Durable Objects 或 WebSocket

### R2.5.8 测试范围

- 普胜、炸清、接清分别正确增加胜者比分和统计
- 双方单局多次犯规正确累计
- 达到抢 N 局、继续比赛和确认结束
- 撤销最后一局和更正历史小局后重新计算
- 比赛中改名不破坏旧流水关联
- 暂停、继续和刷新后的计时恢复
- 页面刷新、断网和异常关闭后的比赛恢复
- 上下二等分、左右二等分的切换、刷新保持和不同屏幕适配
- 中八长图、打印页面和 JSON 导出内容与当前比赛一致
- 追分长图、打印页面和 JSON 包含所有计分、转账、补录、联动及更正事件

### R2.5 验收标准

- [x] 两名玩家可改名并创建抢 N 局或自由局比赛
- [x] 页面结构清楚呈现当前局数、计时、双方比分和四项统计
- [x] 中八计分板可随时切换上下二等分或左右二等分，刷新后保持选择
- [x] 两种分布都严格均分主要区域，并兼容手机横竖屏、平板和桌面端
- [x] 每小局可记录胜者、普胜/炸清/接清、双方犯规和备注
- [x] 所有确认操作立即保存，刷新后可以继续比赛
- [x] 撤销和更正不会删除原流水，所有累计值能正确重算
- [x] 达到目标局数后不会未经确认直接锁死比赛
- [x] 结束后能查看逐局完整流水和更正记录
- [x] 中八战绩长图、打印/PDF 和 JSON 导出信息完整且结果一致
- [x] 追分可导出相同规格的战绩长图、打印/PDF 和 JSON
- [x] 追分导出包含初始分、最终排名及所有计分、转账、卡牌联动、补录和更正流水
- [x] 单手操作时不会因误触大比分区域直接改变比赛结果
- [x] 现有追分、奇招牌和本地历史功能不受影响
- [x] 数据结构已通过 R3 迁移兼容性检查

### R2.5 明确不做

- 不加入裁判判罚规则引擎或自动判断犯规
- 不使用摄像头识别球局
- 不实现大型赛事编排、淘汰表或积分排名
- 不在本版本实现远程实时共同计分
- 不要求登录，不接入云端数据库

---

## 4. R3：账号、云端存储与跨设备同步

目标版本：**v5.0.0**

优先级：**P2**

前置条件：P0 已完成；R2.5 数据模型和本地恢复通过验收

### R3.1 已确认技术决策

- [x] 后端采用 Cloudflare Worker
- [x] 主数据库采用 Cloudflare D1 免费层
- [x] 只使用用户名和密码登录，不接入邮箱验证
- [x] 注册必须填写固定邀请码；不开放公开注册
- [x] 邀请码通过 Worker Secret `REGISTRATION_INVITE_CODE` 配置，不写入 Git、前端或文档
- [x] 邀请码不按时间自动过期，采用常量时间直接比较
- [x] 密码使用 HMAC-SHA256 摘要，密钥放入 `PASSWORD_HMAC_KEY` Worker Secret
- [x] 会话令牌只保存 HMAC 摘要，密钥放入 `SESSION_HMAC_KEY` Worker Secret
- [x] 服务端会话不因时间或长期未登录自动失效
- [x] 用户、对局、预设、牌组和联系人数据不因不活跃自动删除
- [x] 云端对局默认仅本人可见
- [x] R3 采用“单主写设备”策略，不实现多设备离线冲突合并

详细安全口径、数据保留规则和故障边界以现有 [R3 技术决策文档](docs/R3_TECHNICAL_DECISIONS.md) 为准。

### R3.2 实施流程

实施状态（2026-08-11）：

- 阶段 1 的工程、三套 D1、迁移、健康检查、可观测性和秘密扫描已完成；预览与生产 Worker、各自三个真实 Secret、远程迁移和自动化冒烟均已验收。
- 阶段 2 的账号后端闭环及阶段 6 的注册、登录、退出和账号状态前端已完成。
- 阶段 3 的业务表、统一授权函数和首批云端业务 API 已完成，并通过本地及预览环境的 A/B/匿名账号越权、幂等、版本冲突和索引测试。
- 本地数据迁移子阶段已完成：版本化存储适配器、只读预览、完整 JSON 备份与 SHA-256、账号分域稳定 ID、幂等上传、云端链接记录和用户确认面板均已落地；成功、重复、断网、部分失败、取消及同账号异设备恢复测试通过。
- 离线持久队列、指数退避、联网自动补传、手动重试、只读恢复和显式设备接管已完成；服务端按所有权、设备、版本和租约原子校验。
- JSON 数据导出、密码确认账号删除、共享对局保留/解绑以及 30/180 天运行记录定时清理已完成。
- `v5.0.0-rc.1` 已部署至预览 Worker（版本 `9b186551-f875-4e28-9dce-e6b9aa340f15`），健康、D1、匿名权限、客户端功能标识、91 项自动测试及桌面/移动端 24 项 E2E 均通过。

#### 阶段 1：Cloudflare 工程与基础设施

- [x] 新建 Worker 项目并接入现有前端
- [x] 创建本地、预览和生产 D1 数据库及迁移目录
- [x] 配置同源 API 路由、Cookie、安全响应头和写请求 Origin 校验
- [x] 通过 Wrangler 交互式写入预览环境的三个真实 Secret：
  - `REGISTRATION_INVITE_CODE`
  - `PASSWORD_HMAC_KEY`
  - `SESSION_HMAC_KEY`
- [x] 单独写入并验证生产环境的三个真实 Secret
- [x] 建立本地、预览、生产三套隔离配置
- [x] 建立 `/api/health`、D1 校验、Worker 类型生成和可观测性配置
- [x] 在 CI 中扫描仓库与构建产物，阻止 Secret 进入 Git 和产物
- [x] 保留当前 Vercel 版本作为回滚入口

#### 阶段 2：账号与会话

- [x] 实现受邀请码保护的注册接口
- [x] 实现用户名规范化、唯一性检查和保留字规则
- [x] 实现 HMAC-SHA256 密码与会话摘要及常量时间比较
- [x] 实现登录、退出、查看当前会话、修改密码和资料更新接口
- [x] 实现管理员本地重置密码并撤销该账号全部会话的命令
- [x] 会话 Cookie 使用 `HttpOnly`、`Secure`、`SameSite=Lax`
- [x] 浏览器 Cookie 丢失时只要求重新登录，不删除云端数据
- [x] 对注册和登录接口增加基础频率限制和最小化认证审计
- [x] 使用真实 Worker+D1 覆盖注册、登录、会话、改密、限流和资料更新集成测试

#### 阶段 3：D1 数据模型与权限

- [x] 创建用户、会话、统一对局、参与者、事件、联系人和同步设备等 18 张表
- [x] 建立外键、唯一约束及按用户、对局、更新时间和版本号查询的索引
- [x] 对局和事件字段兼容现有追分、奇招牌及 R2.5 中八计分板，并预留 R4 契约
- [x] 建立统一 `requireSession`、`requireMatchRead`、`requireMatchWriteLease` 授权函数
- [x] 当前业务 API 在服务端按 `owner_user_id` 或明确成员关系鉴权
- [x] 当前对局和事件写操作使用 D1 batch、幂等约束和条件更新，防止重复提交
- [x] 使用账号 A、账号 B 和匿名用户覆盖当前业务 API 的越权路径
- [x] 建立 JSON 数据导出、密码确认账号删除和管理员手动重置入口

#### 阶段 4：为 R4 预留的数据与命令协议

R3 不开放实时多人共同写入，但必须提前固定以下契约，避免 R4 重写核心数据：

- 每局拥有稳定的 `match_id` 和单调递增的 `match_version`
- 每个操作拥有客户端生成的 `operation_id`，服务端据此去重
- 每条事件拥有服务端分配的 `sequence_no`
- 参与者与账号分离：`match_player_id` 表示局内选手，`user_id` 表示登录账号
- 预留 `host`、`player`、`spectator` 三种房间角色
- 前端通过业务命令提交修改，不上传并覆盖整份对局状态
- 命令至少包含：`match_id`、`operation_id`、`expected_version`、命令类型和参数
- 牌库抽取、随机结果和最终版本号由服务端决定
- 事件记录保持追加式；更正通过新事件表达，不直接篡改旧事件

R3 中这些字段用于云端持久化、幂等和未来兼容；真正的 WebSocket 房间和并发调度由 R4 实现。

#### 阶段 5：本地数据迁移与跨设备同步

- [x] 首次登录扫描现有本地玩家、预设和对局，包括 R2.5 中八比赛及逐局流水
- [x] 向用户展示待迁移数量和迁移范围
- [x] 用户确认后批量上传，且同一批次可安全重试
- [x] 本地对象保存云端 ID、版本号和最后同步时间
- [x] 离线时允许继续本地单机计分并进入持久化待上传队列
- [x] 恢复联网后自动按序补传，失败指数退避且只删除服务端已确认项目
- [x] 非主写设备默认只读，用户明确接管后才能继续写入
- [x] 接管校验服务端版本、设备归属和租约有效期，版本落后时拒绝并要求刷新

#### 阶段 6：前端入口与状态反馈

- [x] 增加注册、登录、退出和账号状态入口
- [x] 未登录时保留当前本地单机能力
- [x] 显示“仅本地、待同步、同步中、已同步、同步失败、只读”状态
- [x] 提供手动重试、重新登录和从云端恢复入口
- [x] 明确提示跨设备接管和未同步风险

#### 阶段 7：测试、发布与回滚

- 单元测试：规范化、HMAC、邀请码、权限、幂等和版本判断
- 集成测试：注册、登录、退出、迁移、上传、拉取、接管和撤销会话
- 故障测试：断网、重复请求、Worker 重试、D1 失败和 Cookie 丢失
- 兼容测试：现有本地存档、R2.5 中八流水、移动端和无账号模式
- 先发布预览环境，再灰度到少量账号，最后切换生产入口
- 验收后保留旧 Vercel 版本一段观察期

### R3.3 验收标准

- [x] 用户只能凭正确邀请码注册（本地 Worker+D1 集成测试通过）
- [x] 用户名和密码可完成注册、登录与退出（后端 API）
- [x] 密码、会话令牌和邀请码明文不会落入 D1、日志、Git 或构建产物
- [x] 服务端会话和用户数据没有基于不活跃时间的自动失效逻辑
- [x] 同一账号可在另一台设备读取已同步数据（本地 Worker+D1 异设备恢复集成测试通过）
- [x] 旧本地数据及 R2.5 中八流水可以确认后迁移，重复执行不会产生重复记录
- [x] 离线创建的单机对局可在恢复联网后按序补传，刷新和重复请求不会丢失或重复
- [x] 非主写设备不会静默覆盖较新数据（服务端写租约与版本冲突测试通过）
- [x] 用户之间不能读取或修改未授权数据（本地 Worker+D1 A/B/匿名授权测试通过）
- [x] 现有本地模式、构建和核心测试全部保持通过
- [x] R4 所需的版本号、操作 ID、事件序号和角色字段已经落库，并通过幂等、版本冲突和追加事件业务测试

### R3.4 明确不做

- 不在 v5.0.0 引入 Durable Objects 或 WebSocket
- 不允许两台设备同时修改同一场进行中的对局
- 不实现自动合并多设备离线操作
- 不做邮箱、短信、OAuth 或自助找回密码
- 不开放公开注册、公开主页或公共数据浏览
- 不在 R3 同时开发 R4—R6 功能

---

## 5. R4：多人实时共同参加一局

目标版本：**v5.1.0**

优先级：**P3**

前置条件：R3 稳定上线，账号、权限、事件版本和幂等机制通过验收

### R4.1 用户流程

1. 已登录用户创建实时房间，成为 `host`
2. 系统生成短房间码和可分享链接
3. 其他已登录用户通过房间码加入，默认成为 `spectator`
4. 房主可将成员提升为 `player`；提升成功后该云端对局进入成员的主“对局”层，并可从那里重新进入同一房间
5. 房主选择局内选手、玩法、牌组和权限后开始对局
6. 所有在线成员接收同一份快照与后续事件
7. 有权限的成员提交计分、回合、犯规、抽牌等业务命令
8. 服务端按唯一顺序执行并广播确认后的结果
9. 断线用户重连后先取得最新快照，再补齐缺失事件
10. 对局结束后生成不可歧义的最终记录并归档到 D1

远程实时操作默认要求账号登录。未注册的本地玩家仍可作为局内选手，但不能从另一台设备进入房间并写入。

### R4.2 推荐架构

```mermaid
flowchart LR
    A["浏览器 A"] <-->|"WebSocket"| W["Cloudflare Worker"]
    B["浏览器 B"] <-->|"WebSocket"| W
    C["浏览器 C"] <-->|"WebSocket"| W
    W -->|"鉴权后按 match_id 路由"| D["MatchRoom Durable Object"]
    D --> S["DO SQLite：进行中状态与事件"]
    D -->|"结束或检查点"| Q["D1：账号、索引、历史与统计来源"]
```

- 每场进行中的对局对应一个 `MatchRoom` Durable Object
- Worker 负责登录鉴权和把连接路由到正确房间
- Durable Object 负责串行处理命令、分配事件序号、去重和广播
- 进行中状态及关键事件先持久化到 DO 的 SQLite，再向客户端广播成功
- 使用 WebSocket Hibernation API，空闲连接不持续占用活动时长
- D1 保存账号、成员关系、历史索引、已完成对局及后续统计所需投影
- DO 写入 D1 失败时保留待归档状态并重试，不能把未落库结果伪装成已完成

### R4.3 并发与一致性规则

- 客户端只发送业务命令，不发送“用我的整份状态覆盖服务器”
- 每条命令必须携带唯一 `operation_id`，重复到达只执行一次
- 每条命令携带 `expected_version`；版本过旧时拒绝或要求刷新
- Durable Object 为成功事件分配连续 `sequence_no`
- 同一房间中的命令由一个 Durable Object 顺序执行
- 抽牌、洗牌和随机结果由服务端生成，客户端只展示结果
- 房主、玩家、观战者分别执行权限检查
- 每条写命令按 Durable Object 当前成员角色重新鉴权，不得只信任 WebSocket 建连时保存的角色
- 角色变化必须在 DO、D1 对局成员投影、WebSocket attachment/capability 和客户端状态之间收敛；提升触发即时重授权，降级先撤销服务端写权限
- 房间成员、账号对局成员、局内 `match_player_id` 和可选选手位控制绑定分别建模，禁止用用户名或观战关系推断参赛身份
- 撤销或纠错生成补偿事件，不删除已经确认的事件
- 客户端只在收到服务端确认后把操作标记为完成

### R4.4 断线、恢复与异常处理

- 客户端记录最后收到的 `sequence_no`
- 短暂断线重连时请求缺失事件；差距过大时直接下载最新快照
- 共享对局的写入必须在线，断网期间界面进入只读或“等待重连”
- 不把多台设备各自离线产生的共享对局修改自动合并
- Durable Object 被休眠或重新调度后，从持久化状态恢复房间
- 房主离线时允许按规则移交主持权，不能由任意成员自行夺取
- 房间长时间无人连接时可休眠，但对局数据不因休眠而删除
- 已结束房间只读；继续修改必须创建更正事件或新对局

### R4.5 前端体验

- 显示房间码、成员、角色、在线状态和当前操作者
- 显示连接中、已同步、等待确认、已断线和需要刷新状态
- 对暂未确认的命令提供明确的等待反馈，避免重复点击
- 版本冲突时自动刷新并说明哪项操作没有成功
- 房主可管理成员角色、开始/结束对局和移交主持权
- 观战者只能读取，不显示可写操作入口
- 主“对局”层显示本人作为 `host`/`player` 参加的进行中云端对局，并提供进入/重新连接原房间的入口
- `spectator` 只显示在房间/观战入口，不进入个人活动对局、参赛历史或统计

### R4.6 测试与容量验证

- 2、5、15 个客户端同时加入同一房间
- 两个玩家几乎同时提交命令
- 同一 `operation_id` 重发多次
- 客户端断线、换网、刷新页面和后台恢复
- Durable Object 休眠、重启与状态恢复
- 无权限用户伪造房间 ID 或角色
- 当前连接中将观战者提升为玩家，无需手动刷新即可编辑
- 当前连接中将玩家降为观战者，旧连接和伪造请求立即禁写
- 角色切换后刷新、重新登录和多设备连接最终一致
- 玩家提升后主“对局”层出现原云端对局，观战者不进入参赛列表和统计
- D1 暂时失败时的归档重试
- 长时间对局、事件数量增长和快照压缩
- 两台设备共同操作同一个中八计分板并得到一致的逐局流水
- 监控 Durable Objects 请求数、持续时间和存储用量

### R4.7 验收标准

- [ ] 15 个以内在线用户可稳定进入和退出房间
- [ ] 多人同时操作时，所有客户端最终看到完全相同的顺序和状态
- [ ] 重复请求不会重复计分、重复抽牌或重复推进回合
- [ ] 断线重连后无需手工修复即可恢复到最新状态
- [ ] 观战者和无权限成员不能修改对局
- [x] 观战者提升为玩家后无需手动刷新即可编辑；玩家降为观战者后旧连接立即失去写权限
- [x] `host`/`player` 能从主“对局”层进入同一云端实时对局，`spectator` 不被记为参赛者
- [ ] 服务端决定随机结果，客户端不能自行指定抽牌结果
- [ ] 房间休眠或实例重建后不会丢失已经确认的操作
- [ ] 对局结束后可从 D1 读取完整且一致的历史
- [ ] 中八实时房间的比分、胜法、犯规和更正记录与单机版规则一致
- [ ] 免费额度接近阈值时有可观察的指标，而不是突然产生未知费用

### R4.8 费用判断

按 15 人左右的私用规模，Cloudflare Workers、D1 和 SQLite-backed Durable Objects 的免费额度预计足够。主要消耗来自 WebSocket 入站消息、Durable Object 活跃时长和 SQLite 读写；使用休眠 WebSocket、只广播必要事件并减少高频心跳后，通常不需要付费。

上线时仍应设置用量提醒，并在预览环境进行一次 15 客户端压力测试。免费额度与计费政策可能变化，实施 R4 前需再次核对 Cloudflare 官方文档。

参考：

- [Durable Objects 概览](https://developers.cloudflare.com/durable-objects/)
- [Durable Objects 定价与免费额度](https://developers.cloudflare.com/durable-objects/platform/pricing/)
- [WebSocket Hibernation 示例](https://developers.cloudflare.com/durable-objects/examples/websocket-hibernation-server/)

### R4.9 当前实施进度（2026-08-16 更新，2026-08-17 补充实时房间全屏化）

部署状态：预览 Worker `hei8-r3-preview` 已更新至版本 `14d7fb8c-b8d5-495a-8a0b-6e6849ef7abc`；既有生产 Worker `taiqiu-qizhao-cards` 已原地发布 `v5.1.0`（版本 `24bd46dd-2951-4126-8295-288d7ec8678e`），未创建新站点。预览与生产 D1 均已应用 `0004_shiny_quicksilver.sql`，迁移后 `foreign_key_check` 无异常、`quick_check` 为 `ok`。以下 2026-08-16 完成的角色收敛、对局级入口与 WebSocket 101 修复，以及 2026-08-17 完成的实时房间全屏化（纯前端呈现改造），均为本地工作区实现，尚未提交/部署至预览与生产；待本地预览冒烟与 15 客户端容量验证通过后再按 B1—B4 发布。

- [x] 增加一场对局一个 `MatchRoom` 的 SQLite-backed Durable Object 与三环境绑定
- [x] 使用 WebSocket Hibernation API，连接身份通过 attachment 在休眠后恢复
- [x] 房间事件先持久化再广播，并分配连续 `sequence_no` / `version`
- [x] 实现 `operation_id` 幂等、`expected_version` 冲突拒绝和观战者只读校验
- [x] 增加房主创建房间、授权成员读取快照和 WebSocket 连接入口
- [x] 增加 D1 `realtime_rooms` 房间码索引、Drizzle 迁移与 Worker 集成测试
- [x] 增加房间码加入、默认观战者、房主角色分配基础接口和离开房间流程
- [x] 在“我的”页面增加创建/加入房间、成员列表、角色管理和 WebSocket 连接状态入口
- [x] 将追分的普通/自定义计分、转账、黑金、让杆、补录、撤销、更正和当前击球者命令投影接入实时房间
- [x] 将中八的胜负、胜法、犯规、撤销和更正命令投影接入实时房间
- [x] 增加断线指数退避重连、按 `sequence_no` 补事件及超过 200 条差距时的压缩快照回退
- [x] 增加 15 个 WebSocket 客户端同时连接、接收同一顺序广播的本地容量测试
- [x] 增加房主结束对局、D1 原子归档、失败状态与 Durable Object alarm 指数退避重试
- [x] 修复观战者提升为玩家后 WebSocket 身份和客户端编辑权限不收敛的问题，并覆盖反向降级：DO 原地更新目标连接 capability，D1 投影失败返回可重试 `503` 且同一 `operation_id` 收敛，客户端收到自身 `member.role_changed` 立即刷新快照并跳过退避重连；既有连接提升即可写、降级立即 `forbidden`、伪造旧角色重连返回 `403`
- [x] 为 `host`/`player` 建立明确的对局级成员投影，在主“对局”层显示并恢复原实时房间；观战者保持房间级可见：新增 `GET /api/realtime/rooms/mine`，`/api/history` 按对局级成员投影过滤观战者，主“对局”层内嵌实时房间面板并提供云端实时对局就地进入入口
- [x] 修复 HTTP 路由对 WebSocket 101 升级响应重建导致的 `RangeError` 回归（`withRequestId`）：101 响应原样透传，增加经完整路由的 101 升级回归测试，本地预览以原始 WebSocket 客户端验证握手与帧中继
- [x] 实时房间全屏化（2026-08-17，纯前端呈现改造，取代此前“主对局层内嵌实时房间面板 + 就地进入”的呈现）：新增 `/room`（创建/加入入口）与 `/room/:code`（房间对局页）全屏路由，首页“云端实时对局”卡片、设置弹窗创建成功、“我的”页入口统一跳转全屏页；`.room-page`/`.room-topbar`/`.room-dock` 布局对齐 `.match-page`/`.match-banner`/`.match-dock`；WebSocket 生命周期（退避重连、在线恢复、被踢 4004 只读）、角色权限与 `operation_id` 幂等逻辑原样保留，后端 API/DO/协议零改动；配套新增 `e2e/room-fullscreen.spec.ts`（3 条 × 桌面/移动），tsc、77 单元、54 Worker 集成、30 e2e 全部通过（详见 `docs/REALTIME_ROOM_FULLSCREEN_ROADMAP.md`）
- [ ] 在预览环境执行 15 客户端容量与 Durable Objects/D1 用量验证

已确认成员权限规则：新成员输入房间码后默认以 `spectator` 加入，只能由房主提升为 `player`；成员不能自行提权，房主不能直接离开导致房间失去主持人。

---

## 6. R5：牌组、历史、统计与分享

目标版本：**v5.2.0**

优先级：**P4**

状态：**R5.2 战绩删除已收口**（2026-08-16）——删除确认弹窗、云端 `match_user_states` 删除墓碑与幂等 `DELETE /api/matches/:id`、本地墓碑与离线队列清理、共享记录按用户隐藏、同步/补传防复活均已实现并通过集成测试与本地预览实测；牌组系统与统计页仍待规划。

前置条件：R3 云端数据稳定；R4 的实时事件和已完成对局可可靠归档

### R5.1 牌组系统

- 默认牌组
- 自定义牌组
- 牌组复制、删除和设为默认
- 与账号绑定并跨设备同步
- 牌组版本升级与兼容

### R5.2 历史记录

- 按玩法、玩家和日期筛选
- 展示完整计分过程和关键事件
- 区分本地单机、云端单设备和实时多人对局
- 允许对已结束对局追加更正记录

### R5.3 统计页

- 玩家参与次数、胜率和近期趋势
- 常用玩法和牌型分布
- 单局与长期数据分层展示
- 统计数据从已完成对局投影生成，不直接扫描进行中房间

### R5.4 分享

- 生成只读结果页或图片
- 默认不暴露账号资料和未授权历史
- 分享链接可撤销
- 公开分享与房间实时邀请使用不同权限令牌

### R5.5 验收标准

- [ ] 牌组可跨设备读取与维护
- [ ] 历史记录能准确还原不同来源的对局
- [ ] 统计结果可由原始完成事件复算
- [ ] 分享页只暴露用户明确选择的数据

---

## 7. R6：新玩法与娱乐模式

目标版本：**v6.x**

优先级：**P5**

前置条件：核心账号、实时房间、历史和统计链路稳定

候选方向：

- 玩法预设与自定义规则
- 连胜、限时、挑战和随机事件模式
- 赛事或小型联赛
- 更丰富的复盘和观战体验
- 由实际使用反馈决定的实验功能

R6 不在当前阶段锁定具体范围；应根据 R3—R5 的真实使用数据再排序。

---

## 8. 最近完成的实施迭代：R3 离线同步、接管与账号前端

R2.5 / v4.1.0 已发布，R3 本地数据迁移闭环已完成。2026-08-11 已完成预览与生产 Worker 部署、隔离 Secret 配置、远程 D1 迁移、健康检查以及账号/幂等迁移/A-B 授权/退出冒烟；预览测试账号及关联数据已清理，生产错误邀请码冒烟未创建账号。

必须完成：

1. [x] 抽离当前 `localStorage` 访问为版本化存储适配器
2. [x] 扫描本地玩家、预设、牌组、追分对局和 R2.5 中八流水，生成只读迁移预览
3. [x] 用户确认前生成完整 JSON 备份和校验和，失败时不修改原始数据
4. [x] 使用稳定资源 ID 和 `operation_id` 实现可安全重试的批量上传
5. [x] 覆盖成功、重复提交、断网、部分失败和用户取消场景
6. [x] Wrangler 重新登录后配置 preview Secrets、应用远程迁移并完成账号和业务授权冒烟测试

本次不做：

- 注册/登录前端页面
- 完整离线同步和自动接管
- Durable Objects
- WebSocket 实时房间
- 多设备同时修改同一场对局
- 大型赛事管理和摄像头识别

发布门槛已完成：真实邀请码生产账号灰度与生产备份恢复演练均已由产品负责人验收。唯一正式 Worker `taiqiu-qizhao-cards` 已独立配置 `REGISTRATION_INVITE_CODE`、`PASSWORD_HMAC_KEY`、`SESSION_HMAC_KEY`；后续生产发布固定使用该名称。Secret 不可读取，也不得写入仓库。

---

## 9. 总版本表

| 阶段 | 建议版本 | 状态 | 核心交付 |
|---|---:|---|---|
| R1 / P0 | v4.x | 已验收 | CI、生产发布、回滚、兼容性与恢复基线 |
| R2 | v4.0.0 | 已完成 | 本地多人计分、完整规则、牌组快照与事件模型 |
| R2.5 | v4.1.0 | 已发布 | 中八双人计分板、布局切换及中八/追分统一战绩导出 |
| R3 | v5.0.0 | 已验收，正式发布 | 账号、D1、跨设备同步、R4 协议预留 |
| R4 | v5.1.0 | 已部署，待容量观察 | Durable Objects 实时多人共同对局、断线恢复与 D1 可靠归档 |
| R5 | v5.2.0 | 部分实施：战绩删除已收口 | 牌组、历史、统计与分享 |
| R6 | v6.x | 候选 | 新玩法与娱乐模式 |

版本号可在实施前根据实际发布历史调整，但阶段顺序和依赖关系不变。

---

## 10. 每个版本的发布门槛

### 10.1 代码门槛

- lint 通过
- 单元测试和集成测试通过
- 生产构建通过
- 数据库迁移可重复执行并可验证
- 新增关键路径有回归测试

### 10.2 数据门槛

- 旧数据迁移方案已验证
- 重复请求不会产生重复数据
- 失败重试不会破坏状态
- 权限隔离有反向测试
- 对局事件可以复算最终状态
- 中八的比分、胜法和犯规累计必须能从逐局流水重新计算
- 撤销和更正必须保留原事件及关联关系

### 10.3 体验门槛

- 移动端关键流程可完成
- 弱网和断网状态有明确反馈
- 登录失效、只读、冲突和同步失败不会静默发生
- 中八计分板在手机竖屏下可单手完成一局录入
- 中八上下/左右二等分可主动切换，不能仅依赖屏幕方向自动决定
- 大比分显示区域不会因普通误触直接改变比分
- 中八导出必须包含逐局流水，追分导出必须包含完整计分事件流水，不能只有最终结果截图
- R4 起必须清楚显示在线成员、角色和连接状态

### 10.4 运维门槛

- 有预览环境
- 有错误日志和关键用量指标
- 有生产回滚入口
- Secret 不进入源码、构建产物和普通日志
- Cloudflare 免费额度接近阈值时可发现并处理

---

## 11. 暂不纳入近期版本

以下能力暂不进入 R2.5—R5：

- 公开社区或公共主页
- 公开注册
- 支付、订阅或商业化
- 大规模赛事平台
- 跨区域大型房间和复杂匹配系统
- 自动合并多设备离线产生的实时对局分支
- 原生移动应用

若实际用户长期保持约 15 人，应优先保证稳定、简单和可恢复，而不是提前建设大规模平台能力。
