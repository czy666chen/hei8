# 台球奇招产品升级需求与实施路线图

> 文档状态：产品需求基线；交付状态以 `ROADMAP_REMAINING_WORK.md` 为准
> 更新日期：2026-08-11
> 当前产品：台球奇招卡牌抽取网站  
> 当前线上地址：https://hei8chouka.vercel.app

## 1. 项目目标

当前网站已经能够完成奇招卡牌的不放回抽取、共用或双人独立手牌、卡牌使用/跳过、本局记录、自定义排除卡牌以及本机进度保存。

下一阶段的目标，是将网站从单一的“奇招卡牌抽取器”升级为完整的“台球朋友局助手”，覆盖：

1. 赛前选择玩法、玩家、计分规则和牌组。
2. 赛中快速记分、抽牌、使用卡牌、撤销和查看排名。
3. 赛后保存战绩、复盘积分流水和统计玩家表现。
4. 登录后跨设备同步个人资料、常用球友、自定义牌组和历史记录。
5. 同时支持注册玩家和无需注册的临时玩家，降低朋友局的使用门槛。

产品设计原则：

- 优先适配球房内的手机单手操作。
- 不强制所有参与者注册；房主可以快速创建临时玩家。
- 追分计分与奇招抽牌是两个可自由组合的模块，不是互斥玩法。
- 所有分值和关键规则均可配置，不能把地区性玩法写死。
- 每场历史记录必须保留开局时的规则快照，后续修改默认值不能改变旧战绩。
- 对所有卡牌继续提供安全提示和双方同意跳过机制。
- 产品只记录娱乐积分，不提供现金结算、下注或博彩功能。

---

## 2. 信息架构与子页面导航

一级导航控制在五项以内：

| 一级页面 | 建议路径 | 主要职责 | 使用阶段 |
| --- | --- | --- | --- |
| 对局 | `/` | 恢复或操作当前对局 | 比赛中 |
| 玩法 | `/play` | 查看玩法、规则并发起新对局 | 赛前 |
| 牌组 | `/decks` | 管理官方牌组与自定义牌组 | 赛前 |
| 战绩 | `/history` | 查看对局历史和玩家统计 | 赛后 |
| 我的 | `/profile` | 登录、球友、偏好与数据同步 | 平时 |

### 2.1 桌面端导航

桌面端使用顶部导航：

```text
台球奇招    对局    玩法    牌组    战绩                         头像/登录
```

要求：

- 当前页面有明确的选中状态。
- 未登录时右侧显示“登录/注册”。
- 登录后显示头像或昵称，点击进入“我的”。
- 存在未结束对局时，“对局”旁显示进行中标识。
- 新建对局时，如果已有未结束对局，必须提示继续、保存后新建或放弃，不允许静默覆盖。

### 2.2 手机端导航

正常浏览时使用底部导航：

```text
对局 ｜ 玩法 ｜ 牌组 ｜ 战绩 ｜ 我的
```

进入比赛操作状态后，底部优先显示对局操作栏：

```text
撤销 ｜ 主要操作（记分/抽牌） ｜ 更多
```

要求：

- “更多”中可以进入玩家列表、规则摘要、牌组、流水、结束对局等操作。
- 退出沉浸式对局视图后恢复普通底部导航。
- 关键按钮应适合单手点击，避免在球房中需要精细操作。

### 2.3 路由规划

```text
/                         当前对局或快速开始
/play                     娱乐玩法大厅
/play/cards               奇招卡牌玩法介绍
/play/score-chase         追分玩法介绍与开局设置
/decks                    官方牌组与我的牌组
/decks/new                新建自定义牌组
/decks/:deckId            牌组详情/编辑
/history                  历史对局列表
/history/:matchId         对局详情与完整流水
/players                  常用球友与注册玩家关系
/players/:playerId        玩家统计
/profile                  个人中心
/settings                 默认规则与偏好
```

---

## 3. 对局首页

### 3.1 没有进行中的对局

首页展示快速开始入口：

1. 开始奇招卡牌局。
2. 开始追分局。
3. 查看更多娱乐玩法。
4. 使用最近一次配置再开一局。

如果用户有历史记录，可展示最近使用的玩家组合、计分预设和牌组，减少重复设置。

### 3.2 存在进行中的对局

打开网站后直接恢复比赛，不进入宣传页面。顶部显示：

- 当前玩法。
- 已进行时长。
- 参与玩家。
- 当前击球顺序或当前玩家。
- 当前排名/积分。
- 是否启用了奇招牌。
- 当前使用的牌组名称。

主要区域根据玩法切换：

- 纯奇招牌局：显示当前手牌、抽牌区和使用记录。
- 纯追分局：显示玩家排名、记分事件按钮和积分流水。
- 追分 + 奇招牌：同时显示紧凑计分区和当前玩家手牌，手机端可用标签或抽屉切换。

---

## 4. 娱乐玩法大厅

玩法大厅使用卡片式入口。每张玩法卡至少显示：

- 玩法名称。
- 一句话介绍。
- 建议参与人数。
- 预计时长。
- 难度。
- 是否支持奇招牌。
- “查看规则”和“开始设置”按钮。

### 4.1 首期必须实现

#### 4.1.1 奇招卡牌局

保留现有功能并迁移到新的页面框架：

- 51 张实体牌、50 种规则。
- 不放回抽牌。
- 共用手牌或玩家独立手牌。
- 设置起始手牌数量。
- 使用卡牌后进入记录。
- 双方同意后可以安全跳过并补抽。
- 支持撤销上一项操作。
- 支持查看全部卡牌和排除指定卡牌。
- 支持从官方/自定义牌组开局。

#### 4.1.2 多人追分

完整要求见第 5 节。

### 4.2 后续可扩展玩法

#### 双人合作

- 两位玩家共同完成一组目标。
- 以总耗时、完成数量或失误次数计算结果。

---

## 5. 追分系统详细需求

### 5.1 核心原则

- 支持 2–8 名玩家，默认可按常见场景预设为 3 人，但不能限制人数。
- 所有计分项目的分值均可由房主开局前设置。
- 计分事件要适合单手快速录入。
- 支持撤销、补录、编辑备注和查看完整流水。
- 自动维护玩家顺序，但允许房主手动调整。
- 每场对局保存独立规则快照。

### 5.2 开局前必须可配置的计分项目

至少包括：

| 计分项目 | 是否必须支持自定义 | 备注 |
| --- | --- | --- |
| 普胜 | 是 | 分值不能写死 |
| 小金 | 是 | 分值不能写死 |
| 大金 | 是 | 分值不能写死 |
| 黄金 9 | 是 | 可启用/关闭 |
| 普通犯规 | 是 | 可设置扣分或转移分值 |
| 让杆犯规 | 是 | 可启用/关闭 |
| 黑金等地区规则 | 是 | 以可选扩展项目实现 |

额外要求：

- 可设置所有玩家的初始积分，例如全员 0、全员 100 或分别输入。
- 可设置某事件是“得分”“扣分”还是“由其他玩家向获胜者转移分数”。
- 可添加自定义计分项目，填写名称、分值、颜色和说明。
- 可调整计分按钮排序，把最常用的项目放在前面。
- 可保存为个人计分预设，例如“常用球友局”“低分娱乐局”。
- 新建追分局时可以一键套用历史对局或计分预设。

### 5.3 追分开局时选择是否抽牌

追分与奇招牌为可组合模块。开局设置中必须询问：

```text
本场是否加入奇招卡牌？
```

可选项：

1. 不抽牌：纯追分计分。
2. 共用手牌：所有玩家共享同一套手牌。
3. 独立手牌：每名玩家拥有自己的手牌。

启用奇招牌后继续设置：

- 使用哪个官方或自定义牌组。
- 每位玩家/共用区的起始手牌数量。
- 抽牌方式：手动抽牌、每小局抽牌、每轮补牌、使用后自动补牌。
- 每次补牌数量。
- 手牌上限。
- 是否允许双方同意跳过危险卡并补抽。
- 卡牌用完后是否进入弃牌区且本场不再抽到。
- 牌库耗尽后的行为：停止抽牌或经房主确认后重新洗牌。
- 卡牌是否只改变台球规则，或允许影响追分事件。

如果允许卡牌影响积分，需要明确记录影响方式，例如：

- 本次普胜积分翻倍。
- 抵消一次犯规扣分。
- 将某项得分转移给指定玩家。

任何积分影响都必须生成独立的计分流水，不允许只改变总分而不留原因。

### 5.4 追分比赛中的操作界面

主界面建议包含：

1. 当前玩家和下一位玩家。
2. 所有玩家当前积分与排名。
3. 普胜、小金、大金等大按钮。
4. 犯规与更多事件入口。
5. 撤销上一笔。
6. 调整顺序。
7. 本局计时。
8. 最近 3–5 条积分流水。
9. 启用抽牌时显示当前玩家手牌入口。

每次点击计分事件后：

- 先选择事件获益者/责任人；若当前玩家可推断则默认选中。
- 按规则自动计算每名玩家的积分变化。
- 自动更新击球顺序。
- 显示简短结果提示，并提供撤销。
- 生成不可缺失的 `score_event` 流水。

### 5.5 顺序管理

- 开局前拖动调整玩家顺序。
- 支持随机排序。
- 支持胜者开球、固定轮转等顺序策略。
- 大金/黄金 9 等特殊事件是否保持顺序应做成规则配置。
- 开局后允许手动修正当前玩家，但必须写入操作日志。
- 中途加入的玩家记录加入时间，并由房主选择插入位置。
- 已有积分流水的玩家不能直接删除，只能标记中途离场。

### 5.6 撤销与修正

- 支持撤销上一条计分、抽牌、用牌或顺序调整操作。
- 历史流水中的错误记录可以由房主更正。
- 更正时不直接覆盖原记录，应保留原事件和更正关系，便于复盘。
- 已结束的对局默认只读；如需修改，进入“更正模式”并记录修改时间。

### 5.7 结束与结算

结束对局时显示：

- 最终排名。
- 每名玩家最终积分和相对开局变化。
- 普胜、小金、大金、黄金 9、犯规次数统计。
- 最大连胜或最佳表现。
- 使用/跳过的奇招牌数量。
- 总耗时。
- 分享结算卡片。
- 保存战绩。
- 使用本局配置再开一局。

结束前必须二次确认，并处理尚未同步的本地操作。

---

## 6. 玩家添加与身份体系

### 6.1 添加玩家的两种来源

点击“添加玩家”后提供两个标签：

#### 已注册玩家

- 从常用球友中选择。
- 从最近一起玩中选择。
- 输入昵称或用户编号查找。
- 扫描对方二维码添加。
- 其战绩可关联到正式账户。

#### 自定义玩家

- 只填写本局昵称即可参加。
- 可选择头像颜色或座位颜色。
- 不要求注册、不要求登录。
- 可以保存为创建者的本地/云端常用球友。

### 6.2 混合组局

同一场对局允许同时存在：

- 已注册用户。
- 房主保存的非注册常用球友。
- 仅本局有效的临时玩家。

玩家列表必须标明身份，例如：

```text
张三          已注册
台球老王      已注册
小李          本地球友
阿杰          本局玩家
```

### 6.3 身份与数据规则

- 注册玩家使用稳定的 `user_id` 关联，不能依赖昵称。
- 自定义玩家使用独立的 `guest_player_id`，不能只用昵称作为唯一标识。
- 同名自定义玩家默认视为不同的人，除非房主主动选择已有球友。
- 对局中保存玩家当时使用的昵称快照，账号以后改名不影响旧记录展示。
- 注册玩家应通过用户编号、好友关系、邀请链接或二维码添加，避免只靠全局模糊搜索暴露陌生用户。
- 房主可以移除尚未产生记录的玩家。
- 已经产生积分的玩家不能删除，只能标记离场。

### 6.4 战绩认领

后续增加“认领本场战绩”：

1. 自定义玩家之后注册账户。
2. 扫描或打开对局认领链接。
3. 房主确认认领请求。
4. 将该场的 `guest_player_id` 关联到正式 `user_id`。
5. 保留原始昵称快照和认领审计记录。

---

## 7. 牌组系统

### 7.1 官方牌组

首批建议提供：

- 完整奇招：全部 51 张实体牌。
- 轻松欢乐：以语言、互动和轻量干扰为主。
- 技术挑战：翻袋、指定球、指定袋等技术型规则。
- 安全模式：默认排除闭眼、转圈、背后出杆等风险动作。

官方牌组不可直接修改，但用户可以复制为自己的牌组后编辑。

### 7.2 我的牌组

用户可以：

- 新建、重命名、复制和删除牌组。
- 从完整牌库勾选卡牌。
- 按名称、效果、类型和安全标签搜索筛选。
- 设置卡牌数量；需要保证每个实体实例拥有唯一 `instanceId`。
- 创建完全自定义的新卡。
- 设置牌组说明、封面颜色和推荐人数。
- 设置默认起始手牌、补牌方式和手牌上限。
- 将牌组设为私有或通过链接分享（后续）。

### 7.3 卡牌分类建议

- 己方增益。
- 对手干扰。
- 技术挑战。
- 规则改写。
- 社交互动。
- 积分影响。
- 安全敏感。

### 7.4 牌组版本

- 开始对局时保存牌组快照或版本号。
- 后续编辑牌组不得改变已经结束或进行中的对局。
- 历史记录能够还原当时实际使用的卡牌内容。

---

## 8. 战绩与统计

### 8.1 战绩列表

展示：

- 日期和地点（地点可选）。
- 玩法。
- 参与玩家。
- 最终第一名/胜者。
- 对局时长。
- 是否启用了奇招牌。
- 使用的计分预设和牌组。

支持按以下条件筛选：

- 日期范围。
- 玩法。
- 玩家。
- 牌组。
- 已完成/中断状态。

### 8.2 对局详情

必须能够查看：

- 开局规则快照。
- 玩家初始积分和最终积分。
- 最终排名。
- 按时间排序的全部计分流水。
- 抽牌、使用、跳过和补抽记录。
- 顺序调整、玩家加入/离场、更正记录。
- 各计分事件数量统计。
- 使用相同配置再开一局。

### 8.3 玩家统计

注册用户和房主保存的常用球友可统计：

- 总对局数。
- 胜率和第一名次数。
- 平均积分变化。
- 普胜、小金、大金次数。
- 犯规次数。
- 最高连胜。
- 常用玩法和牌组。
- 最近状态趋势。

不同规则预设下的原始积分不宜直接混合比较；统计时应优先展示事件次数、胜率和相对积分变化，并允许按计分预设筛选。

---

## 9. 登录、个人中心与同步

### 9.1 登录策略

建议保持“游客可用、登录后同步”：

- 未登录用户可以创建临时对局和自定义玩家。
- 未登录数据先保存在设备本地。
- 登录后可以选择把本地数据合并到云端。
- 登录不应成为开始朋友局的前置条件。

R3 已完成 v5.0.0 正式版验收，采用以下轻量账号方案：

- 使用用户名和密码，不收集邮箱，也不接入第三方身份服务。
- 注册必须填写服务端固定邀请码，不开放公共注册。
- D1 只保存带独立 Worker Secret 的 HMAC-SHA256 密码摘要，不保存明文或可逆密码。
- 登录使用同源 `Secure; HttpOnly; SameSite=Lax` Cookie，D1 只保存会话令牌摘要。
- 忘记密码由管理员通过本地命令重置，不提供邮件自助找回。

具体安全边界与实现状态见 `docs/R3_TECHNICAL_DECISIONS.md`。

### 9.2 个人中心

- 头像、昵称和用户编号。
- 登录/退出。
- 常用球友。
- 默认玩家顺序策略。
- 默认追分计分预设。
- 默认牌组和抽牌设置。
- 数据同步状态。
- 导出个人数据。
- 注销账户和删除数据。

### 9.3 本地与云端冲突

- 每条记录具有稳定 ID、创建时间和更新时间。
- 本地离线操作进入同步队列。
- 同一场对局只允许一个房主设备进行主要写入，避免多设备同时改分导致冲突。
- 恢复网络后按事件流水同步，而不是仅覆盖最终总分。
- 冲突无法自动解决时提示房主选择版本。

---

## 10. 数据库建议

R3 已确定采用 Cloudflare Worker + D1：Worker 同源承载静态页面和 `/api/*`，D1 保存账号、会话与业务数据。浏览器不能直接访问 D1，所有身份必须从服务端会话推导，所有资源读取和写入必须在 Worker 中执行所有权或成员关系校验。

本地、预览和生产 D1 已建立并完成结构迁移；Vercel 版本暂时保留为回滚入口。预览与生产 Worker、隔离 Secret、远程认证、幂等迁移、离线队列、设备接管、账号前端、导出删除和业务授权均已通过验收；真实邀请码账号灰度与生产备份恢复演练也已完成，v5.0.0 进入正式发布。

### 10.1 建议的数据表

#### `profiles`

- `id`：与认证用户 ID 对应。
- `display_name`。
- `avatar_url`。
- `public_code`：用于精确添加玩家。
- `created_at`。
- `updated_at`。

#### `player_contacts`

- `id`。
- `owner_user_id`：球友列表所有者。
- `registered_user_id`：可空，指向注册玩家。
- `guest_name`：非注册球友昵称。
- `avatar_color`。
- `notes`。
- `created_at`。

#### `score_presets`

- `id`。
- `owner_user_id`。
- `name`。
- `initial_score`。
- `rules_json`：普胜、小金、大金、黄金 9、犯规等配置。
- `is_default`。
- `created_at`。
- `updated_at`。

#### `decks`

- `id`。
- `owner_user_id`：官方牌组可空或使用系统所有者。
- `name`。
- `description`。
- `visibility`。
- `version`。
- `settings_json`。
- `created_at`。
- `updated_at`。

#### `deck_cards`

- `id`。
- `deck_id`。
- `definition_id`：引用官方规则时使用。
- `title_snapshot`。
- `effect_snapshot`。
- `safety_note_snapshot`。
- `card_type`。
- `quantity`。
- `sort_order`。

#### `matches`

- `id`。
- `owner_user_id`。
- `mode`：cards、score_chase、score_chase_cards 等。
- `status`：draft、active、completed、abandoned。
- `rules_snapshot_json`。
- `deck_snapshot_json`：未启用奇招牌时为空。
- `started_at`。
- `ended_at`。
- `location`：可选。
- `created_at`。
- `updated_at`。

#### `match_players`

- `id`。
- `match_id`。
- `registered_user_id`：可空。
- `guest_player_id`：可空。
- `display_name_snapshot`。
- `seat_order`。
- `initial_score`。
- `final_score`。
- `joined_at`。
- `left_at`：可空。
- `claim_status`：战绩认领状态。

必须保证注册 ID 与临时 ID 至少存在一个，但不能把昵称作为外键。

#### `score_events`

- `id`。
- `match_id`。
- `actor_match_player_id`。
- `event_type`。
- `event_label_snapshot`。
- `score_changes_json`：记录每名玩家的变化。
- `related_card_event_id`：可空。
- `note`。
- `occurred_at`。
- `created_by_user_id`。
- `reverses_event_id`：撤销/更正时使用。

#### `card_events`

- `id`。
- `match_id`。
- `match_player_id`：共用手牌时可空。
- `card_instance_id`。
- `card_snapshot_json`。
- `event_type`：draw、play、skip、replace、exclude。
- `occurred_at`。
- `related_score_event_id`：可空。

#### `match_audit_events`

- `id`。
- `match_id`。
- `event_type`：调整顺序、加入、离场、结束、更正等。
- `payload_json`。
- `created_by_user_id`。
- `occurred_at`。

#### `match_claims`

- `id`。
- `match_player_id`。
- `claimant_user_id`。
- `status`。
- `approved_by_user_id`。
- `created_at`。
- `resolved_at`。

### 10.2 权限要求

- 用户只能修改自己创建的计分预设、牌组和球友。
- 对局房主拥有本场主要写权限。
- 参与者只能查看已明确分享给自己的对局。
- 战绩认领必须由房主确认。
- 服务端管理密钥不得进入客户端代码、Git 仓库或公开构建产物。
- 所有公开查询必须限制返回字段，不能暴露邮箱等敏感信息。

---

## 11. 开局流程

### 11.1 追分开局完整流程

```text
选择“多人追分”
→ 添加已注册玩家或自定义玩家
→ 调整玩家顺序
→ 设置每名玩家初始积分
→ 选择/编辑计分预设
→ 设置普胜、小金、大金、黄金 9、犯规等分值
→ 选择本场是否加入奇招牌
→ 如启用，选择共用/独立手牌、牌组和抽牌方式
→ 查看本局规则摘要
→ 开始对局
```

### 11.2 开局确认页

必须清楚展示：

- 玩家和顺序。
- 初始积分。
- 普胜、小金、大金等分值。
- 特殊计分项目。
- 顺序规则。
- 是否抽牌。
- 手牌模式、牌组、起始手牌和补牌规则。
- 规则安全提示。

用户确认后生成不可随意改变的规则快照。比赛中修改规则必须记录到审计流水。

---

## 12. 本地数据迁移

当前版本通过 `localStorage` 保存奇招卡牌本局状态。升级时需要：

1. 继续读取现有 `billiards-trick-cards:v2` 和旧版键值。
2. 将现有进行中的奇招牌局转换为新的本地 `match` 格式。
3. 在用户确认新版本可用前，不立即删除旧键值。
4. 登录后询问是否上传本机牌局和设置。
5. 上传成功并校验后，才标记为已经迁移。
6. 如迁移失败，保留旧数据并提供重试，不允许静默丢失。

浏览器存储可以继续用于：

- 未登录游客的临时对局。
- UI 偏好。
- 离线同步队列。
- 尚未提交的牌组草稿。

登录用户的正式历史、牌组和球友应以数据库为最终数据源。

---

## 13. 安全与体验要求

### 13.1 卡牌动作安全

- 带安全风险的卡牌显示醒目标识。
- 允许双方同意跳过并补抽。
- 场地拥挤、身体不适或球房禁止的动作不应强制执行。
- 安全跳过不作为犯规或失败处理，除非用户在自定义规则中明确设置。

### 13.2 账户安全

- 不自行保存密码。
- 不在前端暴露数据库管理密钥。
- 使用最小权限和行级权限。
- 关键操作进行身份校验。
- 注销和数据删除需要二次确认。

### 13.3 隐私

- 用户可控制牌组和战绩是否分享。
- 临时玩家默认不创建公开资料。
- 注册玩家查找优先使用精确编号、好友或二维码。
- 对局分享链接应支持撤销或过期。

### 13.4 无障碍与移动端

- 交互控件具有可访问名称。
- 支持键盘操作和明确焦点状态。
- 不能只靠颜色区分玩家和事件。
- 尊重减少动画偏好。
- 移动端适配安全区，关键按钮不被浏览器工具栏遮挡。

---

## 14. 实施阶段

### 阶段 0：确认技术与规则

- [x] 确认 R3 采用 Cloudflare Worker + D1，Vercel 版本作为观察期回滚入口。
- [x] 确认采用用户名、密码、固定邀请码和 HMAC-SHA256 的轻量认证方案。
- [ ] 确认首期追分默认规则和分值示例，但所有分值仍可修改。
- [ ] 确认黄金 9、黑金、让杆犯规等项目首期是否默认展示。
- [ ] 确认注册玩家的添加方式：编号、二维码、邀请链接和/或好友列表。

### 阶段 1：应用框架与子页面

- [ ] 增加桌面顶部导航和手机底部导航。
- [ ] 将当前抽卡应用迁移到“对局”页面框架中。
- [ ] 建立玩法、牌组、战绩和我的页面。
- [ ] 增加当前对局恢复和未结束对局保护。
- [ ] 建立统一的空状态、加载状态、错误状态和确认弹窗。

### 阶段 2：追分最小可用版本

- [ ] 实现 2–8 名玩家。
- [ ] 支持注册玩家占位数据和自定义玩家。
- [ ] 支持排序、随机排序、初始积分。
- [ ] 实现普胜、小金、大金可自定义分值。
- [ ] 实现黄金 9、犯规等可选项目。
- [ ] 实现计分预设保存。
- [ ] 实现快速记分、自动轮转、撤销和流水。
- [ ] 实现结束对局和结算页。
- [ ] 首期可先本地保存，但上线账户功能前必须迁移到数据库。

### 阶段 3：追分与奇招牌组合

- [ ] 开局增加“是否抽牌”。
- [ ] 支持共用手牌和玩家独立手牌。
- [ ] 支持手动、每局、每轮和使用后补牌。
- [ ] 支持选择官方/自定义牌组。
- [ ] 卡牌影响积分时生成关联流水。
- [ ] 历史详情同时展示计分和卡牌事件。

### 阶段 4：认证与数据库

- 当前状态：Cloudflare Worker、D1 骨架与账号后端已完成；业务授权、前端和同步待实现。
- [x] 确认并接入 Cloudflare Worker + D1 自建轻量认证方案。
- [x] 建立 18 张数据库表、迁移、外键、唯一约束和基础索引。
- [ ] 建立统一业务权限函数和跨账号反向测试。
- [ ] 实现游客模式与登录后同步。
- [x] 实现个人资料和用户编号后端 API。
- [ ] 增加注册、登录、退出和资料管理前端入口。
- [ ] 实现常用球友和最近玩家。
- [ ] 实现注册玩家搜索/邀请/二维码流程。
- [ ] 实现历史对局云端保存。
- [ ] 实现离线队列和冲突处理。
- [ ] 迁移现有本地牌局数据。

### 阶段 5：自定义牌组

- [ ] 官方预设牌组。
- [ ] 新建、复制、编辑和删除牌组。
- [ ] 卡牌搜索、分类和安全筛选。
- [ ] 创建自定义卡牌。
- [ ] 牌组版本与对局快照。
- [ ] 后续增加分享和导入。

### 阶段 6：战绩与统计

- [ ] 历史对局列表和筛选。
- [ ] 对局详情与完整事件时间线。
- [ ] 玩家胜率、事件次数和趋势。
- [ ] 结算分享卡片。
- [ ] 使用历史配置再开一局。
- [ ] 战绩认领。

### 阶段 7：更多娱乐玩法

- [ ] 双人合作。
- [ ] 每日/每周挑战与主题牌组（可选）。

---

## 15. 首期验收标准

首期可定义为“子页面框架 + 完整追分 + 本地历史”，至少满足：

1. 用户可以在手机上从玩法页开始一场 2–8 人追分局。
2. 添加玩家时可以选择自定义玩家，并为未来注册玩家预留明确入口。
3. 普胜、小金和大金的分数可分别修改。
4. 可选配置黄金 9 和犯规等项目。
5. 开局前可以明确选择是否抽牌。
6. 启用抽牌后可选共用/独立手牌、牌组和起始数量。
7. 比赛中可以快速记分、查看排名和撤销上一笔。
8. 每次分数变化都有可查看的流水，不能只保存总分。
9. 结束时生成排名、事件统计和完整历史。
10. 刷新页面后能够恢复未结束对局。
11. 现有 51 张奇招牌的不放回抽取规则不被破坏。
12. 两张“无懈可击”仍保持不同实体实例，两张同名“落井下石”仍是独立规则。
13. 手机底部操作不会遮挡内容，桌面端导航清楚可用。
14. 危险动作卡可以安全跳过并补抽。

账户与云同步阶段额外验收：

1. 游客无需注册即可开始比赛。
2. 登录用户可跨设备查看自己的牌组、球友和历史。
3. 同一场对局可以混合注册玩家与自定义玩家。
4. 注册玩家通过稳定 ID 关联，改昵称不会丢失历史。
5. 每个用户只能修改自己有权限的数据。
6. 数据库或管理密钥不会出现在浏览器构建产物中。

---

## 16. 测试清单

### 追分逻辑

- [ ] 2、3、8 人均可开局。
- [ ] 普胜、小金、大金采用用户设置的分值。
- [ ] 初始积分非 0 时变化正确。
- [ ] 特殊事件对所有相关玩家的变化正确。
- [ ] 撤销后积分、顺序和统计同时恢复。
- [ ] 中途加入/离场不会破坏排名和历史。
- [ ] 结束后规则修改不会改变旧对局结果。

### 卡牌逻辑

- [ ] 不放回抽取。
- [ ] 共用和独立手牌正确。
- [ ] 每局/每轮/使用后补牌正确。
- [ ] 排除卡和自定义牌组正确。
- [ ] 跳过后补抽正确。
- [ ] 卡牌影响积分时两类流水互相关联。

### 玩家与账户

- [ ] 注册玩家和临时玩家可混合。
- [ ] 同名临时玩家不会错误合并。
- [ ] 注册玩家改名后历史仍正确。
- [ ] 未登录可使用核心功能。
- [ ] 登录后的本地数据合并不会重复。
- [ ] 无权限用户不能读取或修改私有战绩。

### 设备与恢复

- [ ] 刷新恢复进行中对局。
- [ ] 手机竖屏可单手记分。
- [ ] 网络中断时不丢失已完成操作。
- [ ] 恢复网络后事件按顺序同步。
- [ ] 数据迁移失败时旧版本数据仍可恢复。

---

## 17. 暂不纳入首期的事项

以下功能有价值，但不应阻塞第一版追分系统：

- 公开排行榜和赛季系统。
- 现金结算或任何博彩相关功能。
- 复杂社交动态、评论和点赞。
- 实时远程多人共同编辑。
- AI 自动判断进球或摄像头识别。
- 大规模公开用户模糊搜索。
- 牌组交易或付费卡牌。

这些功能需要在核心对局、记分、历史和权限系统稳定后再单独评估。

---

## 18. 当前建议的优先级结论

按照产品价值和实现依赖，推荐顺序为：

1. 先完成子页面导航和新的对局框架。
2. 完成可自定义普胜/小金/大金分值的追分系统。
3. 将“是否抽牌”加入追分开局流程，并复用现有奇招牌逻辑。
4. 接入认证和数据库，支持注册玩家、自定义玩家、球友与云端历史。
5. 完成正式的自定义牌组系统。
6. 补充战绩统计、分享、认领和更多娱乐玩法。

这一顺序能够先交付球房里真正高频使用的功能，同时避免在计分规则和页面结构未稳定前过早搭建复杂社交系统。
