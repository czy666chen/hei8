# 当前版本改进计划

更新时间：2026-08-16

当前基线：R4 / v5.1.0 已部署；预览验收发现“观战者提升为玩家后权限不收敛”和“云端对局未进入主对局层”两项阻断问题，尚待修复及 15 客户端容量与用量验证

建议交付版本：v5.1.1（稳定性热修）→ v5.1.2（实时房间流程补全）→ v5.2.0（战绩删除）

## 执行进度（2026-08-16）

- [x] A1：实时创建已加入五阶段上下文、`requestId`、安全结构化异常日志。
- [x] A2：DO 初始化已加入一次有界重试、可重试 `503`、客户端原房间码恢复入口与故障注入测试。
- [x] A3：中八创建页已移除布局选项；继续采用设备最近布局，局内切换和刷新保持已通过桌面/移动 E2E。
- [x] A4：本地全量回归、预览构建、预览 D1 完整性检查及预览部署已完成（预览版本 `f92a17b9-cf15-47b1-a875-f60d126a4fa3`）；已登录账号冒烟发现角色切换、对局级入口和 WebSocket 连接三处缺陷，暂停生产发布。
- [x] A5：修复观战者提升/玩家降级后的服务端权限、WebSocket 身份和客户端可编辑状态收敛，并完成双向角色切换回归（实现细节见 4.5）。
- [x] A6：让 `host`/`player` 的进行中云端对局出现在主“对局”层，`spectator` 只保留房间级入口；本地预览已通过端到端复现（实现细节见 4.5）。
- [x] 预览冒烟补充发现并修复：`withRequestId`（A1 引入）重建响应时对 WebSocket 101 升级响应抛 `RangeError`，导致所有实时房间连接返回 500、客户端连接状态永远为断线、即使被提升为玩家也无法写入；已改为 101 原样透传并增加 HTTP 路由回归测试（详见 4.1 补充）。
- [x] B1：固定云端直创建请求/响应与成员语义；房主成员与局内临时选手分离（`user_id` 为空即临时选手），踢出仅作用于注册成员，移除选手位仅作用于局内座位（实现细节见 4.2）。
- [x] B2：`POST /api/realtime/rooms/direct` 幂等编排已完成——同一 `operationId` 由 `scopedUuid` 推导确定 `matchId`，重试复用同一对局、房间和选手，不产生重复资源；已通过 HTTP 集成测试与并发竞争路径验证。
- [x] B3：DO 增加 `kickMember`（房主限定、`member.kicked` 事件、关闭被踢者 WebSocket 4004、重复踢出幂等成功）与 `removePlayer`（无流水移除选手位、有流水追加 `player.left` 并置为不活跃、保留历史）；D1 增加 `match_players.kicked_at`/`kicked_by_user_id` 及 `kick`/`unban`/`remove` 路由，`joinRoom` 拒绝被踢成员，`getRoom` 返回被踢列表供房主解除限制；客户端成员菜单增加“踢出/解除限制/移出选手”，被踢连接停止重连。
- [ ] B4：预览环境 2/5/15 客户端容量与 D1/DO 用量验证（R4 收口剩余项）。
- [x] C1：D1 增加 `match_user_states` 用户—对局删除墓碑（`match_id` 无外键，物理删除后仍保留以拦截复活）及索引；新增幂等 `DELETE /api/matches/:id`（房主/参与者校验、进行中与实时房间 409 拦截、重复删除返回“已删除”）；当删除者为房主且无其他可见参与者时直接物理删除 `matches` 及级联事件以降低数据库压力；历史/详情读取排除已删除，补传接口拦截已删除记录（实现细节见 4.4）。
- [x] C2：本地删除墓碑 `billiards-deleted-matches:v1`（刷新/重登保留）、清理待上传同步队列、确认弹窗与列表/详情删除入口；云端删除失败时保留本地删除并在下次同步自动重试。
- [x] C3：共享对局按用户隐藏、其他参与者不受影响且原始事件不变；刷新、重登、跨设备与离线补传均不复活；无权/观战者不能删除；worker 集成测试与单元测试全绿（实现细节见 4.4）。
- [x] 删除链路补强（2026-08-16 本地预览实测补充）：direct 创建成功即记录云端链接、同步 reconcile 时学习“本地 id → 云端 id”映射，使 direct 房间归档战绩与非上传设备上的删除都能到达服务端墓碑（此前只做本地删除，跨设备会复活）；同时修复删除确认弹窗对中八/追分联合类型的状态收窄。

编制依据：[`ROADMAP_REMAINING_WORK.md`](ROADMAP_REMAINING_WORK.md) 的 R4/R5 规划、[`docs/R3_TECHNICAL_DECISIONS.md`](docs/R3_TECHNICAL_DECISIONS.md) 的权限与共享数据保留规则，以及上一会话对实时房间创建故障的生产环境诊断结论。

## 1. 结论摘要

本轮六项问题中，问题 1、5 是阻断 R4 核心入口的稳定性缺陷，应立即处理；问题 6 是实时房间与主对局流程脱节，必须在继续扩展房主管理能力前固定成员投影语义。问题 2 是 R4 用户流程和权限能力的补全，涉及前后端、D1、Durable Object 和 WebSocket，不应按普通页面改动估算；问题 3 是低风险界面简化；问题 4 属于 R5.2 历史记录能力，但涉及本地、云端和共享战绩的数据保留规则，需要用“删除墓碑/按用户隐藏”避免同步后复活。

推荐顺序：

1. 先修复实时房间首次初始化失败，并补充可定位的错误日志。
2. 同一热修版本移除中八创建页的布局选项，保留计分板内切换。
3. 修复角色切换的全链路权限收敛，并让玩家身份产生明确的对局级成员投影和主页面入口。
4. 补齐“创建玩法时直接创建云端实时房间”和房主踢人能力，完成 R4 收口。
5. 在 R5 历史记录阶段增加带确认提示的战绩删除，并处理云端同步和共享记录。

## 2. 评价标准

### 2.1 修改困难程度

| 等级 | 含义 |
| --- | --- |
| D1 低 | 单一页面或局部状态调整，不改数据库和服务端契约 |
| D2 中 | 跨前后端或需要故障恢复、集成测试，但不改变核心数据模型 |
| D3 高 | 涉及数据迁移、同步语义、权限或多个数据来源 |
| D4 很高 | 同时涉及业务生命周期、数据库、Durable Object、WebSocket 和并发一致性 |

### 2.2 急迫性

| 等级 | 含义 |
| --- | --- |
| U0 立即 | 阻断当前已发布核心功能，或会产生大面积不可理解的失败 |
| U1 当前迭代 | 核心流程缺口、权限缺口或用户数据控制能力缺失 |
| U2 同里程碑 | 明显影响体验，但存在可用替代路径且不破坏数据 |

## 3. 问题总表

| 编号 | 问题 | 类型 | 困难度 | 急迫性 | 预计工作量* | 建议版本 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 创建实时房间偶发“服务器内部错误” | 线上稳定性缺陷 | D2 中 | U0 立即 | 2～3 人日 | v5.1.1 |
| 2 | 创建玩法时不能直接选择云端并取得房间号；房主不能踢人 | R4 流程与权限缺口 | D4 很高 | U1 当前迭代 | 5～8 人日 | v5.1.2 |
| 3 | 中八创建页仍要求选择计分板横竖布局 | 界面冗余 | D1 低 | U2 同里程碑 | 0.5～1 人日 | v5.1.1 |
| 4 | 战绩不能删除，且缺少删除确认 | R5 历史与数据控制缺口 | D3 高 | U1 当前迭代 | 3～5 人日 | v5.2.0 |
| 5 | 房主将观战者设为玩家后，刷新仍无法编辑 | R4 权限状态收敛缺陷 | D3 高 | U0 立即 | 2～3 人日 | v5.1.1 |
| 6 | 提升为玩家后，云端对局没有进入主“对局”层 | R4 成员投影与入口缺口 | D3 高 | U1 当前迭代 | 2～4 人日 | v5.1.1 |

\* 工作量包含实现、自动化测试和一次预览环境验收，不包含等待第三方平台故障复现的时间。

## 4. 分项分析与修改方案

### 4.1 实时房间创建偶发服务器内部错误

#### 当前证据

- 生产 D1 已应用创建实时房间所需的 `0004_shiny_quicksilver.sql`，表结构、外键和 Worker 绑定正常。
- 失败请求已成功创建 `realtime_rooms` 记录和房主成员，错误发生在随后调用 Durable Object `initialize()` 的阶段。
- 重试会复用同一房间记录并成功初始化，说明不是账号权限、对局数据或房间码冲突，更符合首次 Durable Object 实例初始化的瞬时失败。
- `MatchRoom.initialize()` 本身已具备幂等恢复基础：发现相同 `match_id` 和 `room_code` 时直接返回现有快照。
- 当前异常处理只记录固定事件名 `realtime_api_failure`，没有错误消息、执行阶段和请求标识，导致生产故障无法直接定位。

#### 困难度与急迫性

- 困难度：D2。核心状态模型不需要重写，但问题只在线上瞬时出现，必须通过故障注入验证恢复路径，而不是只依赖正常集成测试。
- 急迫性：U0。创建房间是 R4 的第一步，统一的 500 会让用户误以为账号、数据或服务器已损坏。

#### 修改方案

1. 将创建流程明确分为 `validate_match`、`allocate_room`、`project_host`、`initialize_do`、`return_snapshot` 五个阶段。
2. 错误日志至少记录 `requestId`、阶段、异常名称/消息、`matchId`、是否复用房间和尝试次数；不得记录 Cookie、会话令牌或完整请求体。
3. 对幂等的 `initialize_do` 增加一次有上限的服务端重试；仍失败时返回可重试的 `503` 和明确提示，不再统一返回“服务器内部错误”。
4. 客户端收到 `503` 时保留当前对局和已有房间码，提供“重新连接房间”动作，禁止再次创建新的云端对局。
5. 增加故障注入测试：第一次 DO 初始化抛错、第二次成功，最终只能存在一个房间码、一个房主成员和一份房间状态。

#### 验收标准

- 首次初始化瞬时失败后能够自动恢复，或给出明确的可重试提示。
- 重试不产生重复房间、重复成员或重复事件。
- 日志能直接区分 D1 分配失败、成员投影失败和 DO 初始化失败。
- 正常创建仍返回 `201`；复用未完成房间返回 `200`；暂时不可用返回 `503`。

#### 预览冒烟补充发现（2026-08-16，已修复）

- `withRequestId`（A1 为响应附加 `X-Request-Id` 时引入）会重建所有响应；WebSocket 升级响应是 HTTP `101` 且携带 `webSocket` 句柄，重建时抛 `RangeError: status codes in the range 200 to 599`。
- 影响：每个实时房间连接请求都返回 500，客户端连接状态永远为“断线”，即使被提升为玩家也无法写入；此前集成测试只直接调用 DO `fetch`，未走 HTTP 路由，故未暴露。
- 修复：`withRequestId` 对 `101` 响应原样透传；新增“HTTP 路由 101 升级”回归测试（经 `SELF.fetch` 完整路由断言 `101` + 首帧快照），并在本地预览用原始 WebSocket 客户端验证握手与帧中继。

### 4.2 创建玩法时直接选择云端版本，并允许房主踢出玩家

#### 当前差距

- 当前创建玩法只生成本地对局；登录用户要先等待对局同步到云端，再到“我的 → 多人实时房间”选择未结束的云端对局，流程被拆成多个页面。
- `POST /api/realtime/rooms` 当前只接收已有的云端 `matchId`，不能直接接收新玩法配置并一次返回房间号。
- 房主当前只能在 `player` 与 `spectator` 之间调整角色，没有踢出接口。
- 非房主只能主动离开。离开后 D1 的 `left_at` 会更新，但再次输入房间码可以重新加入，因此不能直接复用为“踢出”。
- 实时房间成员以注册用户 `userId` 标识；局内临时选手使用 `match_player_id` 且 `user_id` 可为空。这两类对象必须分开管理。

#### 困难度与急迫性

- 困难度：D4。需要把“创建本地玩法、创建云端对局、写入选手、分配房间码、初始化 DO、打开 WebSocket”编排成可恢复的流程，并同时处理注册成员和临时选手两种身份。
- 急迫性：U1。现有替代路径可用，但入口隐蔽、步骤多，且房主缺少基本房间治理能力。

#### 产品与接口建议

创建玩法页增加“对局方式”：

- `本机计分`：保持现有离线流程，游客可用。
- `云端实时房间`：仅登录用户可选；确认创建后直接返回房间码并进入实时计分板。

服务端建议提供一个幂等的“创建云端对局并初始化房间”编排接口，而不是由浏览器串行调用多个接口。请求携带唯一 `operationId`、玩法、规则和选手草稿；响应返回 `matchId`、`roomCode`、房主身份和初始快照。任何阶段失败后，使用同一 `operationId` 重试都必须恢复或复用原资源。

踢出需要区分两种语义：

1. **踢出在线注册成员**：追加 `member.kicked` 事件，更新 D1 成员状态，关闭该用户在房间中的 WebSocket，并阻止其立即用原房间码重新加入；房主可显式解除限制。
2. **移除局内临时选手**：无计分流水时可移除选手位；已有流水后只能追加 `player.left`/`player.removed` 事件并设为不活跃，不能删除其历史事件和旧比分归属。

共同规则：房主不能踢出自己；操作必须经过 `expected_version` 和 `operation_id` 校验；目标已经离开时重复操作返回幂等成功；所有客户端收到同一成员变更事件。

#### 主要修改点

- 创建玩法弹窗：增加本机/云端实时选择及登录状态提示。
- 业务 API：增加原子化的对局草稿与选手落库，或增加专用编排端点。
- 实时 API/DO：增加 `kickMember`、解除限制、移除临时选手和连接关闭逻辑。
- D1：增加明确的踢出/禁止重入状态；不要只依赖 `left_at`。
- 房间界面：成员菜单增加“设为玩家/观战、踢出、解除限制”，危险操作需要二次确认。
- 自动化测试：注册玩家、临时玩家、观战者、重复踢出、旧版本冲突、踢出后重连、房主自踢和已有流水的临时选手移除。

#### 验收标准

- 登录用户在创建玩法时选择云端后，一次确认即可取得房间码并进入房间。
- 任一阶段重试不会创建重复对局、重复房间或重复选手。
- 房主能踢出注册成员和临时选手；非房主不能踢人。
- 被踢注册成员的现有连接立即失去读写权限，重新加入行为符合房主设置。
- 移除已有流水的临时选手不会破坏历史战绩或重算结果。
- 游客仍可完整使用本机计分，不因云端入口加入而退化。

### 4.3 中八创建页移除横竖布局选项

#### 当前差距

创建弹窗当前显示“计分板布局”，但比赛进行中已经有“切换左右/切换上下”按钮，而且布局切换只影响显示、不产生计分事件。开局前要求选择会增加无必要决策。

#### 困难度与急迫性

- 困难度：D1。只需调整创建弹窗和测试；`EightBallMatch.layout` 仍保留，不需要数据迁移。
- 急迫性：U2。不阻断使用，但属于高频入口上的明确冗余。

#### 修改方案

1. 从中八创建弹窗删除“计分板布局”表单项和局部 `layout` 状态。
2. 创建比赛时自动采用本设备最近一次布局；没有历史偏好时默认 `stacked`（上下二等分）。
3. 保留计分板内部“切换左右/切换上下”按钮，并继续保存最近布局偏好。
4. 更新 E2E：创建页不得出现布局选择；开局后两种布局均可切换，刷新后保持。

#### 验收标准

- 中八创建页不再出现横/竖或上下/左右选项。
- 新比赛可以直接开始，默认布局正确。
- 比赛内仍可随时切换布局，切换不增加计分事件且刷新后保持。

### 4.4 增加战绩删除与删除前提示

#### 当前差距

- 本地战绩分别保存在 `history` 和 `eightBallHistory`，页面只有查看、导出和更正，没有删除入口。
- 云端 `/api/history` 只有读取接口，`matches` 也没有面向用户的删除端点或删除状态。
- 客户端会把云端快照重新合并到本地；如果只从本地数组移除，下一次登录、刷新或补传时战绩会再次出现。
- 多人共享对局不能因为某一名参与者删除自己的战绩视图，就破坏其他参与者的记录和追加式事件流水。

#### 困难度与急迫性

- 困难度：D3。页面按钮很简单，真正成本在云端删除语义、同步墓碑、共享记录保留和统计投影。
- 急迫性：U1。用户缺少对错误测试战绩和隐私数据的基本控制能力；但它不阻断当前计分流程。

#### 推荐删除语义

- 删除入口同时放在战绩详情和列表项菜单中。
- 删除前弹出确认框，明确显示玩法、选手、时间，并提示“删除后将从当前账号和本机战绩中移除；共享对局仍会为其他参与者保留”。
- 未同步的纯本地战绩：确认后立即删除，并清理尚未发送的对应同步队列项。
- 已同步的个人战绩：服务端写入删除墓碑，列表、恢复和当前用户统计均忽略该记录。
- 已同步的共享战绩：只对发起删除的用户隐藏/解除关联，其他参与者仍可读取，原始事件不删除。
- 进行中的对局和未归档实时房间不能从战绩页删除，必须先结束或取消。
- 删除请求必须幂等；刷新、重新登录、跨设备恢复和离线补传均不能让已删除战绩复活。

建议使用按用户记录的删除状态（例如 `match_user_states` 或等价表），而不是直接从 `matches` 级联硬删除。这样能同时表达当前用户隐藏、共享记录保留和未来恢复窗口。若后续需要真正物理清除无共享参与者的记录，可在独立保留策略中处理。

#### 主要修改点

- D1 迁移：增加用户—对局删除墓碑/可见性记录及索引。
- 业务 API：增加幂等的 `DELETE /api/matches/:id`，统一校验房主/参与者权限和对局状态。
- 历史查询与详情读取：排除当前用户已删除记录。
- 本地存储/同步：记录删除墓碑，清理待上传项目，并让墓碑优先于较旧云端快照。
- 前端：增加确认弹窗、删除中状态、成功后返回战绩列表和失败重试提示。
- 统计：当前用户统计不再包含其已删除记录；其他参与者统计不受影响。

#### 验收标准

- 删除前必定显示二次确认，取消后数据不变。
- 删除成功后列表、详情、搜索、恢复和统计均不再出现该战绩。
- 刷新、重新登录、跨设备同步和离线补传后，战绩不会复活。
- 共享对局对其他参与者仍完整可见，原始事件与更正记录不被破坏。
- 无权用户不能删除或隐藏不属于自己的战绩。
- 重复删除返回成功或稳定的“已删除”结果，不产生 500。

#### 实现记录（2026-08-16，C1～C3 已完成）

- 云端删除采用“按用户墓碑 + 无共享参与者时物理删除”：`DELETE /api/matches/:id` 先写入 `match_user_states` 墓碑（幂等，重复删除返回 `alreadyDeleted`），再在「删除者为房主且没有其他仍可见的非观战参与者（含已各自删除者）」时直接 `DELETE FROM matches`，由外键级联清理选手、计分/卡牌事件与实时房间等重数据，从而减少 D1 压力；墓碑行不引用 `matches`，物理删除后仍保留，用于拦截离线/跨设备补传复活。
- 读取侧收敛：`requireMatchRead` 与 `/api/history` 对当前用户排除已删除记录（返回 404/不出现在列表）；`/api/migrations/local` 对已删除对局返回 `accepted + deleted: true` 且不重建记录。
- 本地侧：`billiards-deleted-matches:v1` 记录删除墓碑（优先于较旧云端快照）；删除时清理待补传队列项；`reconcileCloudMatches` 过滤已删除 id；云端删除失败保留本地删除并在后续同步自动重试。
- 测试：worker 集成新增 8 个用例（会话/越权 404、个人物理删除与幂等、进行中与实时房间 409、共享保留与按用户隐藏、参与者删除不影响房主、全员删除后物理清理、观战者 403、删除后补传不复活）；单元测试覆盖删除墓碑持久化、队列清理与合并过滤。

### 4.5 角色切换后的权限收敛与对局级显示

#### 当前差距

- 房主修改成员角色后，Durable Object 快照和 D1 成员投影会更新，但已建立的 WebSocket 仍可能保留加入时的角色附件；页面刷新快照并不等于连接身份已经重新授权。
- 客户端可编辑状态同时依赖成员角色和 WebSocket 连接状态。若只刷新成员列表，没有重建或更新连接能力，目标成员仍会停留在只读状态。
- 当前云端实时房间入口主要位于“我的”页面，主“对局”层仍以本地活动对局为中心。成员被提升为 `player` 后没有得到与自建对局一致的继续对局入口。
- 房间成员、账号对局成员和局内计分选手目前容易通过 `user_id`/`match_players` 混用；但三者生命周期不同，不能用“把观战者改成 player”顺便覆盖局内选手身份。

#### 权限收敛规则

1. Durable Object 中的当前房间角色是实时命令权限的权威来源；每条写命令必须按当前成员记录重新校验，不得只信任 WebSocket 建连时保存的角色。
2. `spectator → player` 成功后，服务端追加带新版本号的 `member.role_changed` 事件，并更新 D1 对局成员投影。
3. 对目标用户的现有 WebSocket，要么原地更新 attachment/capability，要么使用专用关闭码要求客户端立即重连并重新鉴权；不得依赖用户手动刷新页面。
4. 客户端收到“自己的角色已变化”事件后，立即获取最新快照；如连接能力版本落后，则跳过普通退避并立刻重连。角色、快照和连接都确认后才显示可写控件。
5. `player → spectator` 必须先在服务端撤销写权限，再通知客户端收起控件；即使旧页面未刷新，后续写命令也必须被拒绝。
6. D1 投影失败不能返回“角色切换完全成功”。流程应进入可重试的待收敛状态，并通过同一 `operation_id` 恢复，避免 DO、D1 和客户端永久分叉。

#### 对局级显示规则

- `host` 和 `player` 都是对局级成员：进行中的云端对局必须出现在主“对局”层，显示“云端实时”标识、房间码、本人角色、连接状态以及“进入/重新连接”动作。
- `spectator` 是房间级成员：可从房间入口观战，但不作为本人进行中的对局显示，也不进入个人参赛统计。
- 从主“对局”层进入时必须恢复同一个 `matchId`/`roomCode`，不能复制成新的本地对局，也不能再次创建房间。
- 账号对局成员只表示访问、操作和列表可见性；局内计分选手继续使用稳定的 `match_player_id`。若玩家需要控制某个选手位，使用独立的 `controlled_match_player_id`（或等价绑定），不得靠用户名猜测。
- 历史/活动对局查询必须按明确的对局级成员投影过滤，而不是把所有尚未离开的观战者都当作参赛者。

#### 验收标准

- 房主提升观战者后，目标客户端无需手动刷新即可在一次事件同步/重连内获得编辑能力；页面刷新和重新登录后仍保持正确。
- 房主将玩家降为观战者后，目标客户端立即只读，旧连接和伪造请求都不能继续写入。
- 角色切换重复请求幂等，DO、D1、WebSocket attachment 和所有客户端最终角色一致。
- 被提升的玩家可在主“对局”层看到该云端对局并进入同一实时房间，显示方式与自建对局一致。
- 观战者不会被错误计入本人的活动对局、历史参赛记录或统计；局内临时选手归属不被角色切换破坏。

## 5. 修改路线表

| 阶段 | 目标版本 | 工作项 | 依赖 | 主要产物 | 退出条件 |
| --- | --- | --- | --- | --- | --- |
| A1 | v5.1.1 | 为实时创建增加分阶段日志、请求标识和安全错误信息 | 无 | 可定位的生产日志 | 能从单条日志确定失败阶段和异常 |
| A2 | v5.1.1 | DO 初始化幂等重试、503 恢复提示和故障注入测试 | A1 | 稳定的房间创建入口 | 首次失败后恢复且无重复资源 |
| A3 | v5.1.1 | 移除中八创建页布局选项，保留局内切换 | 无，可与 A1/A2 同批 | 简化后的创建页 | E2E 覆盖默认、切换和刷新保持 |
| A4 | v5.1.1 | 完成已登录账号预览冒烟并记录角色切换缺陷 | A1～A3 | 可复现的预览验收记录 | 创建/复用链路通过，角色缺陷有稳定用例 |
| A5 | v5.1.1 | 修复角色切换在 DO、D1、WebSocket 和客户端之间的权限收敛 | A4 | 角色变更热修 | 提升无需手动刷新即可编辑，降级立即禁写 |
| A6 | v5.1.1 | 增加云端对局级成员投影和主“对局”层入口，完成预览回归后发布 | A5 | 可恢复的云端活动对局入口 | 玩家可进入同一房间，观战者不进入参赛列表 |
| B1 | v5.1.2 | 固定云端直创建请求/响应，以及房间成员、对局成员、局内选手和控制绑定语义 | A6 | API 与状态契约 | 评审通过且测试用例可枚举 |
| B2 | v5.1.2 | 实现开局即创建云端对局和房间的幂等编排 | B1 | 一次确认返回房间码 | 失败重试不产生孤儿或重复资源 |
| B3 | v5.1.2 | 实现踢出、禁止重入、断开连接和临时选手离场 | B1、B2 | 完整房主管理能力 | 权限、重连、冲突和历史保留测试通过 |
| B4 | v5.1.2 | 预览环境 2/5/15 客户端验收及 D1/DO 用量观察 | B2、B3 | R4 收口报告 | 路线图 R4 容量项可勾选 |
| C1 | v5.2.0 | 增加战绩删除墓碑、查询过滤和幂等删除 API | B4；R4 归档稳定 | 云端删除能力 | 共享/个人/无权限场景通过 |
| C2 | v5.2.0 | 增加本地删除、同步防复活和确认弹窗 | C1 | 完整用户删除流程 | 刷新、重登、跨设备和离线补传均不复活 |
| C3 | v5.2.0 | 历史、恢复、统计和共享回归 | C2 | R5.2 删除验收记录 | 当前用户删除生效，其他参与者数据不受影响 |

## 6. 建议测试矩阵

| 场景 | 单元测试 | Worker/DO 集成测试 | 浏览器 E2E | 预览环境 |
| --- | --- | --- | --- | --- |
| DO 首次初始化失败后恢复 |  | 必须 | 必须 | 必须 |
| 房间创建重试不重复 |  | 必须 | 必须 | 必须 |
| 当前连接中观战者提升为玩家 | 必须 | 必须 | 必须 | 必须 |
| 当前连接中玩家降为观战者 | 必须 | 必须 | 必须 | 必须 |
| 角色切换后刷新、重登和多设备收敛 | 必须 | 必须 | 必须 | 必须 |
| 提升后主“对局”层显示并进入原房间 | 必须 | 必须 | 必须 | 必须 |
| 观战者不进入活动对局、历史参赛和统计 | 必须 | 必须 | 必须 | 必须 |
| 本机/云端创建方式切换 | 必须 | 必须 | 必须 | 必须 |
| 房主踢注册成员 |  | 必须 | 必须 | 必须 |
| 房主移除临时选手 | 必须 | 必须 | 必须 | 建议 |
| 被踢成员保持连接/重新加入 |  | 必须 | 必须 | 必须 |
| 中八局内布局切换与刷新保持 | 必须 |  | 必须 | 冒烟 |
| 删除纯本地战绩 | 必须 |  | 必须 |  |
| 删除个人云端战绩 | 必须 | 必须 | 必须 | 必须 |
| 删除共享战绩 | 必须 | 必须 | 必须 | 必须 |
| 删除后跨设备/离线补传不复活 | 必须 | 必须 | 必须 | 必须 |

## 7. 主要风险与控制措施

| 风险 | 结果 | 控制措施 |
| --- | --- | --- |
| D1 已创建但 DO 初始化失败 | 留下孤儿房间或重复房间 | 以 `operationId` 和 `matchId` 幂等复用，允许从任一阶段恢复 |
| DO 角色已更新但 WebSocket attachment 仍是加入时角色 | 玩家刷新后仍只读，或降级后旧连接仍可写 | 命令按 DO 当前角色重新鉴权；角色事件携带能力版本，并更新 attachment 或强制立即重连 |
| DO 角色已更新但 D1 对局成员投影失败 | 房间内可编辑，主“对局”层却找不到对局 | 角色切换使用可重试的收敛流程和统一 `operation_id`，未完成时暴露明确状态并自动补偿 |
| 把观战房间成员直接当作参赛者 | 观战对局污染历史、统计和活动对局列表 | 分离房间成员、对局级成员、局内选手及可选控制绑定；查询按成员类型过滤 |
| 只改 D1 成员状态但旧 WebSocket 仍在线 | 被踢用户继续收到或提交命令 | DO 内删除成员、主动关闭其连接，并在每条命令再次校验成员身份 |
| 把临时选手和在线账号当成同一身份 | 历史归属错误、无法正确踢出 | 明确区分 `match_player_id` 与 `user_id`，分别建模选手位和房间成员 |
| 直接硬删除有流水的选手或共享对局 | 破坏追加式账本和其他玩家历史 | 使用离场/删除墓碑事件，原事件流保持不可变 |
| 只删除本地数组 | 云端同步后战绩复活 | 本地和云端都保存删除墓碑，合并时墓碑优先 |
| 删除后统计仍包含旧战绩 | 页面结果互相矛盾 | 统计投影以当前用户可见的已完成对局为准，并增加复算测试 |

## 8. 涉及代码范围

- `app/GameApp.tsx`：创建玩法、中八布局、房间成员管理、角色切换后的连接重授权、主“对局”层云端入口、战绩列表/详情和确认弹窗。
- `worker/realtime/api.ts`：房间创建恢复、分阶段日志、踢出接口和权限。
- `worker/realtime/match-room.ts`：当前角色逐命令鉴权、WebSocket attachment 更新/重连、成员踢出事件、幂等和版本冲突。
- `worker/business/api.ts`：云端对局级成员查询、活动对局入口、云端直创建编排、战绩删除和历史过滤。
- `db/schema.ts` 与新迁移：明确的对局级成员投影、可选选手位控制绑定、踢出/禁止重入状态、战绩删除墓碑及索引。
- `src/lib/local-storage.ts`、`src/lib/cloud-sync.ts`、`src/lib/cloud-reconcile.ts`：本地删除、离线队列和防复活合并规则。
- `worker/realtime/*.test.ts`、`worker/business/api.integration.test.ts`、`e2e/core-flows.spec.ts`：回归与验收测试。

## 9. 完成定义

本轮改进不以“页面上出现按钮”为完成，而以以下结果全部成立为准：

1. 实时房间创建发生瞬时平台错误时可恢复、可解释且不产生重复数据。
2. 观战者提升为玩家后无需手动刷新即可编辑；玩家降为观战者后旧连接立即失去写权限。
3. `host`/`player` 的进行中云端对局出现在主“对局”层并能恢复原房间，`spectator` 不污染参赛历史和统计。
4. 登录用户能从创建玩法入口直接得到云端房间码，游客的离线能力不退化。
5. 房主能安全管理注册成员和临时选手，被踢成员不能继续使用旧连接写入。
6. 中八布局只在计分板内切换，创建页不再要求选择。
7. 战绩删除经过确认，本地、云端、共享、统计和跨设备同步语义一致。
8. v5.1.2 完成预览环境 15 客户端容量与用量验证，正式关闭 R4 剩余验收项后再进入 R5.2 收口。
